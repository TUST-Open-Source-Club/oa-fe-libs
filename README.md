# club-oa-fe-libs

社团 OA 前端共享库（独立仓库，挂载于主仓库 `packages/`）。

| 包 | 说明 |
| --- | --- |
| `packages/ui` | 设计系统组件库 + Tailwind token（Web 与客户端共用） |
| `packages/core` | API SDK、Pinia stores、WS 实时、平台适配层 |
| `packages/config` | ESLint / TS / Tailwind 共享配置 |

> 状态：待初始化。
